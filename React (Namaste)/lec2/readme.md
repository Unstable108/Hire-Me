install react and react-dom

npm i react
npm i react-dom

run 
npx pacel index.html (entry point)
npx means execute using npm

React -> core package
ReactDOM -> package for manipulating dom

we have to tell browser that it is not a simple script file, it is a module file
via type="module"

