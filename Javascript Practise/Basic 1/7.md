The Definitions

call(): Invokes a function immediately, forcing its this context to be the object you pass as the first argument, followed by arguments passed individually (comma-separated).

apply(): Invokes a function immediately, forcing its this context to be the object you pass as the first argument, followed by arguments passed together as an array (or array-like object).